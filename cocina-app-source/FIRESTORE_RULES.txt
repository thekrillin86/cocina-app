==========================================================
REGLAS DE FIRESTORE — proyecto cocina-juanlc
==========================================================

Las reglas deciden quién puede leer y escribir en la base de datos.
Se publican a mano en la consola de Firebase:

    console.firebase.google.com
      → proyecto cocina-juanlc
      → Firestore Database
      → pestaña Rules / Reglas
      → pegar, Publicar

Este fichero lleva los identificadores con hueco (UID_DE_JUAN y
compañía) porque el repositorio es público. Los de verdad se pegan
solo en la consola. No son secretos —saber un identificador no deja
entrar a nadie, hace falta la contraseña— pero tampoco pintan nada
en GitHub.


==========================================================
1. ESTADO ACTUAL — cerrado, desde el 13/09/2026
==========================================================

Hecho y comprobado:

  · Cuentas de correo y contraseña para Juan, Magda y el asistente
  · AuthGate entra con signInWithEmailAndPassword, ya no hay PIN
  · Publicadas las reglas del punto 3, por identificador de usuario
  · Acceso anónimo DESACTIVADO en la consola

A partir de aquí, a la base de datos solo llegan esas tres cuentas, y
la del asistente únicamente puede dejar lotes en el buzón: no lee, no
cambia y no borra nada.

OJO con la cuenta del asistente: se creó para que el asistente de
Cowork dejara lotes en un buzón que ya NO EXISTE en la app. Es una
credencial activa sin dueño: hay que borrarla (ver punto 5).


==========================================================
2. LA PUESTA EN MARCHA, PASO A PASO  (ya hecha, se deja
   escrita por si hay que repetirla en otro proyecto)
==========================================================

EL ORDEN IMPORTA. Si se publican las reglas del punto 3 antes de que
existan las cuentas y esté desplegado el acceso nuevo, nadie puede
entrar en la app.

Nunca se pierde el control: la consola de Firebase se abre con la
cuenta de Google, no con la app. Si algo sale mal, se vuelve a entrar
en la consola y se deshacen las reglas.

  PASO 1 — Crear las cuentas                        (en la consola)
    Authentication → Sign-in method
      · Activar «Correo electrónico/contraseña»
      · Dejar APAGADO el inicio de sesión sin contraseña
    Authentication → Users → Add user
      · Una cuenta para Juan, con correo real
      · Una cuenta para Magda, con correo real
      · Una cuenta para el asistente; el dominio da igual
    Apuntar el User UID de cada una.

  PASO 2 — Desplegar el acceso nuevo                    (en el repo)
    AuthGate.jsx pasa de PIN a correo y contraseña.
    Las reglas siguen siendo las del punto 1, así que si algo falla
    la app sigue funcionando.

  PASO 3 — Comprobar que se entra                       (en el móvil)
    Cerrar la app del todo y volver a abrirla. Debe pedir correo y
    contraseña, y dejar pasar. Hasta que esto funcione en los dos
    móviles, NO seguir.

  PASO 4 — Publicar las reglas del punto 3             (en la consola)
    Con los identificadores de verdad puestos.

  PASO 5 — Cerrar el acceso anónimo                    (en la consola)
    Authentication → Sign-in method → Anónimo → Desactivar.
    Solo cuando el paso 4 esté comprobado.


==========================================================
3. LAS REGLAS FINALES
==========================================================

Sustituir los tres huecos por los identificadores reales.

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Las dos personas de la casa
    function esDeCasa() {
      return request.auth != null && request.auth.uid in [
        'UID_DE_JUAN',
        'UID_DE_MAGDA'
      ];
    }

    // La cuenta que usa el asistente, solo para dejar lotes
    function esElAsistente() {
      return request.auth != null && request.auth.uid == 'UID_DEL_ASISTENTE';
    }

    // El PIN ya no se usa. Cerrado del todo.
    match /config/{doc} {
      allow read, write: if false;
    }

    match /menus/{weekId}          { allow read, write: if esDeCasa(); }
    match /shopping-lists/{listId} { allow read, write: if esDeCasa(); }
    match /recipes/{recipeId}      { allow read, write: if esDeCasa(); }

    // El buzón: el asistente solo puede DEJAR lotes, con esta forma
    // exacta. Ni los lee, ni los cambia, ni los borra.
    match /inbox/{loteId} {
      allow create: if esElAsistente()
        && request.resource.data.keys().hasOnly(
             ['createdAt','source','batch','status','recipes'])
        && request.resource.data.status == 'pending'
        && request.resource.data.createdAt is string
        && request.resource.data.source is string
        && request.resource.data.source.size() <= 60
        && request.resource.data.batch is string
        && request.resource.data.batch.size() <= 60
        && request.resource.data.recipes is list
        && request.resource.data.recipes.size() > 0
        && request.resource.data.recipes.size() <= 50;

      allow read, update, delete: if esDeCasa();
    }

    // Todo lo demás, cerrado
    match /{document=**} { allow read, write: if false; }
  }
}


==========================================================
4. COMPROBARLAS ANTES DE FIARSE
==========================================================

Consola → Firestore Database → Rules → pestaña Playground:

  · get de /menus/2026-37, autenticado con el UID de Juan
      → debe PERMITIRSE
  · el mismo get sin autenticar
      → debe DENEGARSE
  · create en /inbox/prueba con el UID del asistente y un documento
    que tenga createdAt, source, batch, status 'pending' y recipes
      → debe PERMITIRSE
  · get de /menus/2026-37 con el UID del asistente
      → debe DENEGARSE

Y en la app: Más → Buzón debe decir «El buzón está vacío» en vez de
avisar de que faltan las reglas.


==========================================================
5. EL BUZÓN, RETIRADO
==========================================================

Este punto describía cómo el asistente de Cowork dejaría lotes de
recetas en /inbox por la API REST. No funcionaba y el buzón se ha
quitado de la app.

POR QUÉ NO FUNCIONABA
  Comprobado el 13/09/2026: el entorno donde corre ese asistente sale
  a internet por un proxy que deniega por política de la organización
  firestore.googleapis.com, identitytoolkit.googleapis.com y hasta el
  propio dominio de Netlify. Devuelve 403 al conectar. No era cuestión
  de permisos de Firebase ni de credenciales: la petición no salía de
  allí. Y aparte, un asistente de chat no debe manejar contraseñas.

QUÉ HAY QUE HACER
  Borrar la cuenta del asistente, que ya no la usa nadie:
    Consola → Authentication → Users → la cuenta del asistente
      → los tres puntos → Eliminar usuario

  Las reglas publicadas pueden quedarse como están. El bloque de
  /inbox seguirá ahí sin que nadie lo use, y al borrar la cuenta nadie
  puede escribir en él: `esElAsistente()` compara contra un
  identificador concreto y Firebase no los reutiliza. Falla cerrado.

  Si algún día se quiere limpiar del todo, basta con quitar de las
  reglas el bloque `match /inbox/{loteId}` y la función
  `esElAsistente()`. No corre prisa.

CÓMO ENTRAN LAS RECETAS
  Por la pantalla Importar, que acepta una lista de recetas. Se puede
  abrir la app en el navegador del ordenador, más cómodo para pegar un
  JSON largo que el móvil.


==========================================================
6. VUELTA ATRÁS DE EMERGENCIA
==========================================================

Si algún día las reglas del punto 3 dejan a todo el mundo fuera —por
ejemplo, si se borra por error una de las cuentas y hay que
recrearla—, esto devuelve el acceso mientras se arregla. Abre la base
de datos a cualquiera que se autentique, así que es un parche
temporal, no un sitio donde quedarse.

Requiere volver a activar el acceso anónimo SOLO si además hay que
usar una versión antigua de la app; con la versión actual basta con
las cuentas de correo.

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /menus/{weekId}          { allow read, write: if request.auth != null; }
    match /shopping-lists/{listId} { allow read, write: if request.auth != null; }
    match /recipes/{recipeId}      { allow read, write: if request.auth != null; }
  }
}
